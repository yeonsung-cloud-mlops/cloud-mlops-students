# 01주차 — MLOps 개요 & 클라우드 첫걸음

**클라우드 MLOps** · 연성대학교 · 230분 (10분 조기 종료)
NCS: `2001070101_18v1.1` 인공지능 플랫폼 동향 분석하기(부분) / `2001070201_18v1.3` 인공지능 서비스 기술 환경 분석하기(부분) / `2001070101_18v1.5` 인공지능 플랫폼 구축 비용 계획하기(부분)

---

## 0. 회차 요약 (강사용 1페이지)

| 항목 | 내용 |
|---|---|
| 학습목표 | AWS access portal로 로그인하고, 서울 리전에서 개인 S3 버킷을 만들어 CloudShell에서 파일을 올리고 내릴 수 있다. |
| 오늘의 결과물 | ① 개인 S3 버킷 `mlops-2026-<학번>` (폴더 `raw/` `processed/` `models/` 포함) ② CloudShell `aws s3 ls` 실행 결과 스크린샷 ③ MFA 활성화 화면 스크린샷 |
| 사전 준비 | IAM Identity Center 사용자·그룹·학생용 권한 세트 구성 / AWS access portal URL 배포 / Budgets 알람 설정 / 인스턴스 유형 제한 정책 확인 / **교실 PC와 학교 Wi-Fi에서 access portal·콘솔·CloudShell 접속 사전 테스트** / MFA 안내문 인쇄 / 실습용 샘플 CSV 준비 |
| 학생 준비물 | 노트북(충전기 포함), 학번, 학교 이메일 주소, **스마트폰**(MFA 인증 앱용), 필기도구 |
| 예상 사고 지점 | ① **MFA 앱 미설치** — 스마트폰에 Google Authenticator/Microsoft Authenticator가 없고 앱스토어 로그인이 안 되는 학생이 매 반 3~5명 ② **학교망에서 콘솔 접속 지연** — `console.aws.amazon.com` 로딩이 30초 이상 걸리거나 CloudShell이 "Failed to start" ③ **리전을 버지니아(us-east-1)로 두고 실습** — 버킷을 엉뚱한 리전에 만들고 2주차에 EC2가 안 보인다고 함 ④ **버킷 이름 전역 중복 오류** — `mlops-2026` 같은 짧은 이름을 넣어 `BucketAlreadyExists` |

### 시간표

> ⚠ **이 회차만 표준 시간표를 쓰지 않는다.** 계정 발급·로그인·MFA·브라우저 문제로 시간이 반드시 샌다.
> 이론을 45분 → 30분으로 줄이고, **개별 트러블슈팅 버퍼 45분**을 별도로 확보했다.

| 시간 | 구성 | 분 |
|---|---|---|
| 00:00–00:08 | **후킹 체험** — “이 노트북, 중고로 얼마에 올릴까?” 학생 예상 → 예측 서비스 실행 | 8 |
| 00:08–00:15 | **완성 형태 해설** — 사용자 화면·API 응답·운영 화면을 차례로 공개 | 7 |
| 00:15–00:25 | **오리엔테이션** — 15주 결과물, 평가 방법, 팀 구성, 비용 정책 | 10 |
| 00:25–00:55 | **이론** — MLOps란 무엇인가 + 15주 로드맵 한 장 + 클라우드 기본 개념 | 30 |
| 00:55–01:05 | 휴식 | 10 |
| 01:05–01:50 | **실습 A** — 콘솔 로그인 → MFA 설정 → 리전 서울 고정 → IAM 권한 확인 → Budgets 알람 확인 | 45 |
| 01:50–02:00 | 휴식 | 10 |
| 02:00–03:00 | **실습 B** — S3 버킷 생성, 폴더 규칙, 업로드/다운로드, CloudShell `aws s3 cp/ls` 반복 | 60 |
| 03:00–03:45 | **체크포인트 제출 + 개별 트러블슈팅 버퍼** | 45 |
| 03:45–03:50 | 리소스 정리 안내 | 5 |
| **합계** | | **230** |

> **버퍼 운용 지침**: 45분 버퍼는 "남는 시간"이 아니라 **예정된 시간**이다.
> 진도를 다 마친 학생은 먼저 퇴실시키지 말고 **부록 A 치트시트로 CLI 3회 반복**을 시키거나 **팀 구성 희망서를 그 자리에서 작성**하게 한다.
> 조교는 이 시간에 미제출자 명단을 실시간으로 좁혀 나간다.

---

## 1. 도입 — 학생 참여형 후킹과 오리엔테이션 (25분)

### 1-1. 시작하자마자 가격부터 묻는다 (0~3분)

강사는 과목명이나 MLOps 정의를 먼저 설명하지 않는다. 아래 조건만 화면에 띄운다.

> 새 제품 가격 150만 원 / 3년 사용 / 배터리 78% / 외관 B등급  
> **여러분이라면 중고 거래 앱에 얼마로 올리겠습니까?**

- 학생은 휴대폰 메모장이나 종이에 가격 하나만 적는다.
- 앞줄과 뒤쪽에서 각각 2명에게 답을 듣고 칠판에 적는다.
- 정답을 맞히는 활동이 아니므로 근거를 길게 묻지 않는다.
- 이 단계에서는 “머신러닝을 배워봤나요?”처럼 경험을 확인하는 질문을 하지 않는다.

### 1-2. 예측 서비스 직접 실행 (3~8분)

강사는 인터넷 연결과 관계없이 실행되는 파일을 연다.

1. [`../실습/week01/hook-demo/index.html`](../실습/week01/hook-demo/index.html)을 브라우저로 연다.
2. 학생이 제시한 가격을 아직 공개하지 않고 입력값 그대로 **예상 가격 보기**를 누른다.
3. 사용 기간을 1년과 5년으로 바꿔 결과가 달라지는 것을 보여준다.
4. 오른쪽 JSON에서 `request`, `response`, `model_version` 세 단어만 짚는다.

**강의 멘트**
> 방금 여러분은 모델을 공부한 것이 아니라 **서비스를 사용했습니다.**  
> 왼쪽은 사람이 쓰는 화면이고, 오른쪽은 프로그램이 주고받는 답입니다.  
> 이번 학기에는 이 숫자를 계산하는 모델만 만드는 데서 멈추지 않습니다. 다른 사람이 입력하고 결과를 받을 수 있는 화면과 주소까지 만듭니다.

> 지금 이 계산이 정확한지는 중요하지 않습니다. 우리가 15주 동안 해결할 질문은 세 가지입니다.  
> **어디에 올릴까? 고친 내용을 어떻게 다시 배포할까? 문제가 생긴 것을 어떻게 알아챌까?**

### 1-3. 완성 형태를 세 화면으로 공개 (8~15분)

강사는 한 번에 구조도를 설명하지 않고 아래 순서로 화면을 넘긴다.

1. **사용자 화면** — 값을 입력하고 결과를 받는 페이지
2. **API 응답** — 같은 요청에 JSON으로 답하는 `/predict`
3. **운영 화면** — 요청 수, 오류, 응답 시간을 확인하는 CloudWatch

**강의 멘트**
> 사용자는 첫 번째 화면만 봅니다. 하지만 서비스를 만드는 사람은 뒤의 두 화면까지 책임져야 합니다.  
> **이 과목은 AI 이론을 많이 외우는 과목이 아니라, 다른 사람이 실제로 눌러볼 수 있는 결과물을 만드는 과목입니다.**

> 15주 뒤에는 팀마다 “제가 만든 모델입니다”라고 파일을 보여주는 대신, **“여기 주소로 들어가서 직접 눌러보세요”**라고 말하게 됩니다.

### 1-4. 오늘의 작은 성공으로 연결 (15~18분)

1. AWS 콘솔 → S3 → 강사 버킷 `mlops-2026-demo`를 연다.
2. `raw/`, `processed/`, `models/` 폴더를 보여준다.
3. CloudShell에서 `aws s3 ls s3://mlops-2026-demo/`를 실행한다.

**강의 멘트**
> 완성 화면을 한 번에 만들지는 않습니다. 오늘은 그 서비스가 사용할 파일을 둘 **온라인 보관함 하나**를 각자 만듭니다.  
> 수업이 끝날 때 여러분 이름의 S3 버킷과 파일 하나가 보이면 오늘은 성공입니다.

### 1-5. 경험을 묻지 않는 사전 진단 (18~20분)

손들기 대신 아래 세 문장을 화면에 띄우고 학생이 손가락 1~3으로 현재 상태를 표시하게 한다.

1. **처음 봐도 화면을 따라 누르면 할 수 있다.**
2. **명령어가 나오면 조금 긴장된다.**
3. **로그인이나 설치부터 도움이 필요하다.**

2번과 3번 학생이 섞이도록 짝을 정한다. 잘하는 학생을 공개적으로 도우미로 지정하지 않는다.

### 1-6. 오리엔테이션 본문 (20~25분)

**강의 스크립트**
> 방금 본 화면을 팀별로 하나씩 만드는 과목이 **클라우드 MLOps**입니다. 처음 듣는 말이어도 괜찮습니다.  
> 파이썬이나 머신러닝 경험이 서로 달라도 시작할 수 있도록 완성 예제와 매주 이어받을 수 있는 기준 파일을 제공합니다.
>
> 우리가 만드는 결과물은 세 가지가 연결된 형태입니다. **예측하는 프로그램, 다른 사람이 쓰는 화면, 문제가 생기면 확인하는 운영 화면**입니다.  
> 모델 성능 경쟁이 아니라 이 세 부분이 다시 실행되고, 설명되고, 안전하게 정리되는지를 평가합니다.
>
> 운영 방식을 말씀드리겠습니다. 매주 4시간이지만 **230분에 끝냅니다.** 10분 일찍 마칩니다. 대신 **마지막 5분은 리소스 정리 타임**이고, 이건 빠지면 안 됩니다. 이유는 잠시 후에 무섭게 말씀드리겠습니다.
> 이론은 짧게 갑니다. 이론 대 실습이 3 대 7입니다. 개념 설명은 여러분이 실습하다가 뭔가 터졌을 때 붙여서 하겠습니다. 그게 훨씬 잘 남습니다.
>
> 그리고 중요한 원칙 하나. **제가 먼저 처음부터 끝까지 만들어 보여드립니다.** 여러분 팀은 제가 만든 뼈대에 **여러분 데이터만 갈아 끼웁니다.** 여러분이 설계할 것은 주제와 데이터 두 가지뿐이고, 구조는 그대로 복제하시면 됩니다. 그러니까 "나는 못 하겠는데" 하는 걱정은 지금 접으셔도 됩니다.

**평가 방법 (판서/슬라이드 요점)**

| 항목 | 배점 | 언제 |
|---|---|---|
| 출석 및 참여 | 10 | 매주 |
| 주차별 실습 체크포인트 | 25 | 12개 회차 × 약 2점 (스크린샷/URL 제출) |
| M1 문제정의서 | 10 | 4주차 |
| M2 중간 시연 | 15 | 8주차, 팀별 5분 |
| 최종 산출물 (코드·문서·배포) | 25 | 15주차 |
| 최종 발표 + 상호평가 | 15 | 15주차 |

- **감점**: 리소스 미정리 적발 시 회당 **-2점** / 개인정보·저작권 위반 데이터 사용 시 프로젝트 점수 **0점**
- 체크포인트는 **그날 수업 안에서** 제출하는 것이 원칙이다. 밀리면 다음 주에 두 배로 힘들어진다.

**팀 구성 안내**
- 3~4인 1팀. **3주차에 확정**하고 15주차 발표까지 그대로 간다.
- 오늘 과제로 **팀 구성 희망서**를 낸다. 같이 하고 싶은 사람 이름과 관심 도메인 1순위를 적는다.
- 미배정자는 강사가 3주차에 배정한다. 친한 사람이 없어도 불이익 없다.
- 역할은 데이터 / 모델 / 서빙 / 운영·문서 네 가지를 권장하되, **각자 최소 1회는 전 구간을 혼자 돌려봐야 한다.** 개인 체크포인트로 확인한다.

**⚠ 비용 정책 경고 (반드시 이 톤으로)**

**강의 스크립트**
> 여기서부터는 농담 안 섞고 말씀드립니다. 우리가 쓰는 AWS는 학과 카드로 실제 돈이 나가는 계정입니다. 무료 체험판 아닙니다.
> 학기 예산은 정해져 있지만 실제 비용은 인스턴스 유형, 사용 시간, 리전, 모델에 따라 달라집니다. 오늘 강사 화면에서 예산과 현재 사용액을 함께 확인하겠습니다.
>
> 서버를 켜놓고 집에 가면 실행 시간만큼 비용이 계속 늘어납니다. 정확한 단가는 수업 당일 AWS 가격표와 Cost Explorer에서 확인합니다. 중요한 것은 숫자를 외우는 것이 아니라, 사용하지 않는 리소스를 바로 중지하는 습관입니다.
> 그래서 여러분 계정마다 **월 10달러 예산 알람**을 걸어두었습니다. 50%, 80%, 100%에서 여러분 학교 메일로 알람이 갑니다. 이 메일이 오면 무시하지 마세요. 저한테도 같이 옵니다.
>
> 그리고 여러분이 받는 학생 역할에는 **큰 서버를 만들 수 없는 제한**이 있습니다. 허용된 CPU 인스턴스 유형만 선택할 수 있고 GPU 서버는 만들 수 없습니다. 이 과목은 CPU로 짧게 학습되는 모델을 사용합니다.
>
> 마지막으로, 매주 수업 끝나기 5분 전에 **리소스 정리 타임**을 합니다. 조교가 돌아다니면서 여러분 인스턴스가 꺼졌는지 눈으로 확인합니다. 안 끄고 가면 회당 2점 감점입니다. 출석 점수가 10점인데 5주 방치하면 그만큼 날아갑니다.

**학생 질문 예상 & 답변**
- Q: 제 개인 AWS 계정으로 해도 되나요? → A: 안 됩니다. 평가와 비용 추적이 학과 계정 태그 기준으로 이뤄집니다. 개인 계정으로 하면 체크포인트 확인이 안 돼서 점수를 못 드립니다.
- Q: 비용이 넘으면 제가 물어내야 하나요? → A: 아닙니다. 학과에서 부담합니다. 다만 방치가 반복되면 감점이고, 심하면 계정을 일시 정지합니다.
- Q: 머신러닝을 잘 못하는데 괜찮나요? → A: 이 과목은 **모델 정확도로 점수를 주지 않습니다.** 파이프라인이 끝까지 도는지, 문서를 보고 재현되는지를 봅니다. 루브릭 25점 중 정확도 항목은 0점입니다.

---

## 2. 이론 (30분)

> ⚠ 이 회차는 이론이 30분뿐이다. **욕심내지 말 것.** 아래 세 소주제를 각 10분에 끊는다.
> 학생이 질문을 길게 하면 "그건 N주차에 그 화면 보면서 설명하겠다"로 넘기고 진도를 지킨다.

### 2-1. 방금 본 화면 뒤에서는 무슨 일이 일어났나 (10분)

**강의 스크립트**
> 조금 전에 가격을 입력하고 버튼을 눌렀습니다. 화면 뒤에서는 입력을 받고, 정해진 계산을 실행하고, 결과를 돌려줬습니다.  
> 한 사람의 노트북에서 한 번 실행하는 것은 어렵지 않습니다. 여러 사람이 같은 주소로 들어와도 동작하게 만들고, 수정한 버전을 다시 올리고, 오류를 찾는 순간부터 일이 많아집니다.
>
> MLOps는 여기서 나온 말입니다. ML(머신러닝) + Ops(Operations, 운영). 우리말로 하면 **"머신러닝 모델을 실제로 굴리기 위한 운영 기술"** 정도입니다.
> 그런데 이걸 개념으로 외우면 하나도 안 남습니다. 그래서 저는 이렇게 정의하겠습니다.
> **MLOps = 모델이 노트북을 떠나 다른 사람 손에 닿기까지 필요한 모든 잡일을 자동화하는 것.**
>
> 비유를 하나 들겠습니다. 머신러닝 모델 만들기가 **"맛있는 요리를 한 접시 만드는 일"**이라면, MLOps는 **"그 요리를 식당에서 매일 같은 품질로 제공하는 일"**입니다.
> 한 접시 만드는 거랑 300그릇 파는 건 완전히 다른 문제죠. 재료를 어디서 매일 받을지(데이터 파이프라인), 레시피를 어떻게 기록할지(실험 관리), 주방을 어떻게 똑같이 복제할지(컨테이너), 손님 주문을 어떻게 받을지(API), 오늘 몇 그릇 나갔고 불만은 없었는지(모니터링). 이게 전부 MLOps입니다.
>
> 하나만 더. 실무에서 데이터 과학자가 시간을 어디에 제일 많이 쓸까요? 모델 튜닝? 아닙니다. **데이터 준비와 운영**에 대부분을 씁니다. 모델 코드는 전체 시스템에서 아주 작은 조각입니다. 여러분이 이번 학기에 체감하게 될 겁니다.

**판서/슬라이드 요점**
- MLOps = ML + Operations. "노트북을 떠난 모델을 굴리는 기술"
- 핵심 문제 3개: ① 제 컴퓨터에선 되는데요 ② 3주 전 그 결과 어떻게 냈지 ③ 어제까진 잘 됐는데
- 모델 코드는 전체 시스템의 일부일 뿐 — 나머지가 전부 MLOps 영역
- 이 과목의 평가 기준은 **정확도가 아니라 "끝까지 도는가"**

**학생 질문 예상 & 답변**
- Q: DevOps랑 뭐가 다른가요? → A: DevOps는 코드만 다룹니다. MLOps는 코드 + **데이터 + 모델**을 같이 다룹니다. 데이터가 바뀌면 결과가 바뀌기 때문에 훨씬 까다롭습니다.
- Q: 이거 취업에 도움 되나요? → A: 채용 공고에 "MLOps 엔지니어"라는 직무가 따로 있습니다. 그리고 신입 데이터 분석가도 요즘은 "본인이 만든 모델을 API로 띄워봤나"를 묻습니다. 여러분 15주차 결과물이 그 답이 됩니다.

### 2-2. 15주 로드맵 한 장 (10분)

**강의 스크립트**
> 이제 지도를 한 장 보여드리겠습니다. 이 그림은 앞으로 **매주 첫 슬라이드에 다시 나옵니다.** 매주 "우리 지금 여기입니다" 하고 위치를 찍어드릴 겁니다. 길을 잃었다 싶으면 이 그림만 보세요.

```
[1주] 계정·저장소        [2주] 서버 환경
   AWS 로그인 · S3          EC2 · 리눅스 · Python
        │                        │
        └────────┬───────────────┘
                 ▼
        [3주] 데이터 파이프라인 ── 🏁 팀 프로젝트 착수
            raw/ → processed/
                 ▼
        [4주] 베이스라인 모델 ───── 🏁 M1 문제정의서
            학습 → 평가 → S3 저장
                 ▼
        [5주] 실험 관리 (MLflow)
            뭘 어떻게 바꿔서 얼마나 좋아졌나 기록
                 ▼
        [6주] 컨테이너 (Docker·ECR)
            "제 컴퓨터에선 되는데요" 해결
                 ▼
        [7주] 서빙 API (FastAPI)
            모델을 HTTP 주소로
                 ▼
        [8주] 배포 + 데모 화면 ───── 🏁 M2 중간 시연
            외부에서 접속되는 URL
                 ▼
     [9주] SageMaker    [10주] Bedrock
       관리형 ML 체험      생성형 AI 붙이기
                 ▼
        [11주] 자동 배포 (GitHub Actions)
            push 하면 알아서 반영
                 ▼
        [12주] 모니터링 (CloudWatch) ─ 🏁 M3 파이프라인 완성
            로그 · 지표 · 알람
                 ▼
        [13주] 통합·문서화·리허설
                 ▼
        [15주] 최종 발표 ─────────── 🏁 M4
```

> 이 그림에서 딱 하나만 기억하세요. **1주차부터 13주차까지가 한 줄기입니다.** 중간에 옆길로 새는 게 아니라, 여러분이 1주차에 만든 S3 버킷에 3주차 데이터가 들어가고, 그 데이터로 4주차 모델을 만들고, 그 모델이 7주차 API가 되고, 그게 8주차에 주소를 갖습니다.
> 그러니까 **1주차를 빼먹으면 3주차가 안 됩니다.** 오늘 만드는 버킷 이름을 꼭 적어두세요.

**판서/슬라이드 요점**
- 1~2주 = 도구 준비 / 3~5주 = 데이터·모델 / 6~8주 = 패키징·서빙 / 9~10주 = 관리형 서비스 체험 / 11~12주 = 자동화·운영 / 13~15주 = 마무리
- 마일스톤 4개: M1(4주) M2(8주) M3(12주) M4(15주)
- 팀 프로젝트 총 공수 18~20시간 (수업 내 12h + 팀 자율 6~8h)
- **오늘 만드는 S3 버킷이 학기 내내 쓰인다**

**학생 질문 예상 & 답변**
- Q: 중간에 한 주 결석하면 따라갈 수 있나요? → A: 됩니다. 회차마다 **완성본 저장소 태그**(`week-01-done` 형식)를 드립니다. 결석한 주는 완성본에서 이어받으면 됩니다. 다만 그 주 체크포인트 점수는 못 드립니다.
- Q: 딥러닝은 안 하나요? → A: 이 과목에서는 CPU로 10분 안에 학습되는 모델만 다룹니다. 딥러닝을 배우는 과목이 아니라 **모델을 서비스로 만드는 과목**이기 때문입니다.

### 2-3. 클라우드 기본 개념 (10분)

**강의 스크립트**
> 마지막 10분은 클라우드 용어 네 개만 잡고 갑니다. 이것만 알면 오늘 실습은 됩니다.
>
> 첫째, **리전(Region)**. 우리말로 "지역"입니다. AWS 컴퓨터들이 실제로 놓여 있는 지구상의 위치입니다. 서울에도 있고, 도쿄에도 있고, 미국 버지니아에도 있습니다.
> 여기서 중요한 것 — **리전이 다르면 완전히 다른 세상입니다.** 서울 리전에 만든 서버는 버지니아 리전 화면에서는 **안 보입니다.** 없어진 게 아니라 다른 방에 있는 겁니다.
> 매년 이걸로 사고가 납니다. 콘솔 오른쪽 위에 지역 이름이 뜨는데, 기본값이 버지니아인 경우가 있어요. 그 상태로 서버를 만들고 다음 주에 "제 서버가 사라졌어요" 합니다.
> 그래서 우리는 **한 학기 내내 서울, `ap-northeast-2` 하나만 씁니다.** 실습 A에서 제일 먼저 할 일이 이걸 고정하는 겁니다.
>
> 둘째, **가용영역(Availability Zone, AZ)**. 리전 안에 있는 실제 건물, 즉 데이터센터입니다. 서울 리전 안에 네 개쯤 있습니다. 왜 나눠놨을까요? 건물 하나에 불이 나거나 정전되도 나머지가 살아있게 하려고요. 우리 수업에서는 크게 신경 쓸 일이 없습니다. "리전 안에 건물이 여러 개 있구나" 정도만.
>
> 셋째, **온디맨드(On-Demand)**. "쓴 만큼 낸다"는 뜻입니다. 서버를 켜둔 시간만큼 초 단위로 과금됩니다. 이게 클라우드의 핵심이자 여러분이 조심해야 할 이유입니다.
> 비유하면 **호텔방**입니다. 집을 사는 게 아니라 방을 빌리는 겁니다. 체크아웃을 안 하면 계속 요금이 붙습니다. **인스턴스 Stop이 체크아웃입니다.**
> 하나 더. 서버를 껐어도 **저장장치(EBS) 요금은 계속 나갑니다.** 호텔로 치면 방은 비웠는데 짐 보관함은 계속 빌리고 있는 셈입니다. 그래서 학기 말에는 아예 삭제까지 합니다.
>
> 넷째, **책임 공유 모델(Shared Responsibility Model)**. AWS가 어디까지 책임지고 우리가 어디부터 책임지는지를 나눈 겁니다.
> AWS는 **"클라우드 자체의 보안"**, 즉 건물, 전기, 하드웨어, 네트워크 선을 책임집니다. 우리는 **"클라우드 안에서의 보안"**, 즉 비밀번호를 어떻게 관리하는지, 어떤 포트를 열어놨는지, 데이터를 아무나 볼 수 있게 해뒀는지를 책임집니다.
> 오늘 MFA를 설정하는 게 바로 우리 쪽 책임입니다. 비밀번호 하나 털리면 남이 여러분 계정으로 GPU 서버 100대를 켤 수 있습니다. 실제로 벌어지는 일입니다.

**판서/슬라이드 요점**
- **리전** = 지구상 위치. 우리는 `ap-northeast-2`(서울) 고정. 리전 다르면 리소스가 안 보인다
- **가용영역(AZ)** = 리전 안의 데이터센터 건물
- **온디맨드** = 호텔방. 켜둔 만큼 과금. Stop = 체크아웃. 단 **EBS(짐 보관함)는 계속 과금**
- **책임 공유 모델** = AWS는 건물·전기·하드웨어, 우리는 비밀번호·권한·포트
- 오늘 우리가 지는 책임: **MFA 켜기**

**학생 질문 예상 & 답변**
- Q: 왜 하필 서울인가요? 미국이 더 싸지 않나요? → A: 조금 쌉니다. 하지만 거리가 멀면 응답이 느리고, 무엇보다 **전원이 같은 리전을 봐야 서로 도와줄 수 있습니다.** 리전 섞이면 트러블슈팅이 지옥이 됩니다.
- Q: 리전을 바꾸면 지금까지 만든 게 지워지나요? → A: 아니요, 안 지워집니다. **안 보일 뿐**입니다. 서울로 돌아오면 그대로 있습니다.
- Q: S3 버킷도 리전이 있나요? → A: 있습니다. 버킷은 만들 때 리전을 정하고 나중에 못 바꿉니다. 이름은 전 세계에서 유일해야 하지만, 실제 데이터는 특정 리전에 놓입니다.

---

## 3. 실습 A — AWS 콘솔 로그인 · MFA · 리전 고정 · 권한/예산 확인 (45분) · 공통 예제 따라하기

**목표** AWS access portal로 로그인하고, MFA와 서울 리전을 확인한 뒤, 현재 역할과 예산 알람을 확인한다.

**사전 배포 파일**
- `01_계정정보.txt` — 개인별로 다르게 배포. 내용 예시:
```text
AWS access portal URL : https://<조직별주소>.awsapps.com/start
사용자 이름           : 학교 이메일 또는 학번 계정
초기 비밀번호         : (개별 배포)
권한 세트             : MLOpsStudent
고정 리전         : ap-northeast-2 (아시아 태평양(서울))
```
- `01_MFA설치안내.pdf` — 인증 앱 3종 설치 방법 + 앱 설치 불가 시 대안

> ⚠ **강사 운영 팁**: 시작 전에 AWS access portal URL을 크게 적어둔다. 루트 사용자 로그인 화면으로 들어가지 않게 한다.

### 수행 순서

**① 콘솔 로그인 (10분)**

1. 브라우저는 **Chrome 또는 Edge**를 쓴다. 시크릿 모드/프라이빗 창을 권장한다(다른 AWS 계정 세션과 섞이는 사고 방지).
2. 주소창에 배포받은 AWS access portal URL을 그대로 입력한다.
```text
https://<조직별주소>.awsapps.com/start
```
3. 학교 이메일 또는 배포받은 사용자 이름과 초기 비밀번호를 입력한다.
4. 최초 로그인이면 **비밀번호 변경 화면**이 뜬다. 새 비밀번호를 정하고 **반드시 메모**한다.
   - 조건: 8자 이상, 대문자·소문자·숫자·특수문자 각 1개 이상
   - 권장 형식 예: `Mlops!<학번뒤4자리>2026`
- 확인 포인트: access portal에 AWS 계정과 `MLOpsStudent` 권한 세트가 보이면 성공.

**② 리전을 서울로 고정 (5분)**

1. 콘솔 오른쪽 위, 계정 이름 왼쪽에 있는 **지역 선택 메뉴**를 클릭한다.
2. 목록에서 **`아시아 태평양(서울) ap-northeast-2`** 를 선택한다.
3. 주소창 URL에 `region=ap-northeast-2` 가 들어있는지 눈으로 확인한다.
4. 이 상태에서 **브라우저 북마크로 저장**한다. 이름은 `AWS-서울`.
- 확인 포인트: 오른쪽 위에 "서울"이라고 한글로 표시된다. 매 수업 시작 시 이걸 먼저 본다.

> ⚠ 리전 표시가 "버지니아 북부" 또는 "N. Virginia"이면 **지금 하는 모든 작업이 헛수고**다. 옆 사람 것도 서로 봐준다.

**③ MFA(다단계 인증) 설정 (15분)**

> MFA(Multi-Factor Authentication, 다단계 인증) = 비밀번호에 더해 **1분마다 바뀌는 6자리 숫자**를 하나 더 요구하는 잠금장치.

1. 스마트폰 앱스토어에서 인증 앱을 설치한다. 셋 중 아무거나.
   - Google Authenticator / Microsoft Authenticator / Twilio Authy
2. 콘솔 오른쪽 위 **계정 이름 클릭 → 보안 자격 증명(Security credentials)** 으로 들어간다.
3. **다단계 인증(MFA)** 섹션에서 **MFA 디바이스 할당**을 누른다.
4. 디바이스 이름에 `phone-<학번>` 을 입력하고, **인증 관리자 앱(Authenticator app)** 을 선택한 뒤 다음.
5. 화면에 QR 코드가 뜬다. 폰의 인증 앱에서 `+` → **QR 코드 스캔**으로 찍는다.
6. 앱에 6자리 숫자가 뜨기 시작한다. **연속된 두 개**를 입력한다.
   - 첫 번째 코드 입력 → 숫자가 바뀔 때까지 기다림 → 두 번째 코드 입력
7. **MFA 추가** 클릭.
8. 로그아웃 후 다시 로그인해서 6자리 코드를 요구하는지 확인한다.
- 확인 포인트: 보안 자격 증명 화면의 MFA 목록에 `phone-<학번>` 이 나타나면 성공. **이 화면을 캡처**한다. (체크포인트 제출물 3번)

> ⚠ **폰에 앱을 못 깔면** (앱스토어 로그인 불가, 저장공간 부족 등)
> → Chrome 확장 프로그램 방식의 인증기 또는 강사 예비 기기를 사용한다.
> → 그래도 안 되면 **오늘은 MFA 미설정으로 넘어가고 다음 주 시작 전까지 해결**한다. 조교가 명단에 기록한다. MFA 때문에 오늘 실습 전체가 멈추면 안 된다.

**④ 내 역할과 권한 확인 (8분)**

1. 오른쪽 위의 계정 메뉴를 열어 현재 계정과 역할 이름을 확인한다. (예: `MLOpsStudentRole`)
2. 강사가 공유한 **AWS 액세스 포털**로 돌아가 내가 받은 계정과 권한 세트를 확인한다.
3. CloudShell에서 `aws sts get-caller-identity`를 실행해 `Account`와 `Arn`을 확인한다.
4. `Arn`에 `assumed-role/MLOpsStudentRole` 형태가 보이면 임시 자격 증명으로 접속한 것이다.
- 확인 포인트: 권한은 사용자의 고정 키가 아니라 로그인할 때 선택한 역할에서 온다.

**강의 스크립트 (이 단계에서 붙이는 설명)**
> 지금 확인한 **역할과 권한 세트**가 "이 수업에서 무엇을 해도 되는지"를 정합니다.
> 아파트 출입카드 같은 겁니다. 여러분 카드는 우리 동, 우리 층만 열립니다. 옥상 기계실은 안 열립니다. 여기서 `t3.medium`까지만 허용된 게 그 기계실 잠금이라고 보시면 됩니다.
> 실습하다가 "AccessDenied"라는 빨간 글씨가 뜨면 여러분이 뭘 잘못 친 게 아니라 **권한이 없는 겁니다.** 그때는 저를 부르세요.

**⑤ Budgets 예산 알람 확인 (7분)**

1. 콘솔 검색창에 `Billing and Cost Management` 입력 → 이동.
2. 왼쪽 메뉴에서 **Budgets(예산)** 클릭.
3. 본인 예산 `mlops-2026-<학번>-monthly` 를 찾아 클릭한다.
4. 다음 세 가지를 확인한다.
   - 예산 금액: **$10 / 월**
   - 알림 임계값: **50% / 80% / 100%**
   - 수신 이메일: 본인 학교 메일 주소가 맞는가
5. 메일 주소가 틀렸으면 손을 든다. (학생 권한으로는 수정 불가 — 강사가 처리)
- 확인 포인트: 현재 사용액이 $0.00 ~ $0.5 사이면 정상.

> ⚠ Billing·Budgets 화면은 학생 역할에 비용 조회 권한이 없으면 보이지 않는다. 안 보이면 오류가 아니므로 강사 화면의 학급 예산과 알림 설정을 확인한다.

### ⚠ 여기서 막히면

| 증상 | 원인 | 조치 |
|---|---|---|
| AWS 홈에서 로그인했더니 루트 사용자 이메일을 물어봄 | 학교용 AWS 액세스 포털이 아닌 일반 로그인 페이지로 들어감 | 강사가 배포한 액세스 포털 URL로 다시 접속하고 북마크한다 |
| 로그인은 되는데 화면이 30초 넘게 하얗게 뜸 | 학교망에서 AWS 콘솔 CDN 응답 지연 | ① 시크릿 창으로 재시도 ② 광고차단 확장 끄기 ③ 그래도 느리면 **폰 핫스팟으로 전환** — 이게 가장 확실. 조교가 핫스팟 가능 학생을 미리 파악해 짝 지어준다 |
| MFA QR 스캔 후 코드가 "잘못됨"으로 거부됨 | 폰 시계와 서버 시계가 어긋남 | 폰 설정 → 날짜 및 시간 → **자동 설정 켜기** 후 재시도. Google Authenticator는 앱 설정의 "시간 보정" 실행 |
| 폰에 인증 앱 설치 불가 | 앱스토어 로그인 실패, 저장공간 부족 | 브라우저 확장 인증기 사용 또는 **오늘은 건너뛰고 명단 기록 → 다음 주 전까지 해결** |
| 콘솔 오른쪽 위가 "버지니아 북부" | 리전 기본값 | 지역 메뉴에서 **아시아 태평양(서울)** 선택 후 북마크 갱신. 이미 만든 리소스가 있으면 서울에서 다시 만든다 |
| `AccessDenied`가 나옴 | 현재 역할에 해당 작업 권한이 없음 | 오류 문구와 `aws sts get-caller-identity` 결과를 강사에게 보여 준다 |
| Budgets 메뉴가 안 보임 | 학생 역할에 비용 조회 권한이 없음 | 강사 화면으로 학급 예산과 알림을 확인하고 넘어간 |

### 컷오프 안내

**45분 경과 시 강제 종료.** ①②는 전원 필수, ③④⑤는 미완료여도 실습 B로 넘어간다.
MFA·Budgets 미완료자는 조교가 명단에 적어두고 **트러블슈팅 버퍼(45분)** 에서 개별 처리한다.
실습 B(S3)는 MFA 없이도 진행 가능하므로, **MFA 때문에 반 전체를 세우지 않는다.**

---

## 4. 실습 B — 개인 S3 버킷 만들고 CLI로 다루기 (60분) · 각자 직접 수행

**목표** 본인 이름의 S3 버킷을 만들고 표준 폴더 구조를 세운 뒤, 콘솔과 CloudShell CLI 두 가지 방법으로 같은 파일 업로드/다운로드를 해본다.

**과제 지시문** (학생에게 그대로 읽어줄 문장)

> 지금부터 60분 동안 여러분 이름이 붙은 **개인 저장소**를 하나 만듭니다.
> 이 저장소는 오늘 만들고 끝나는 게 아니라 **15주 내내 씁니다.** 3주차 데이터도 여기 들어가고, 4주차 모델 파일도 여기 들어가고, 13주차 로그도 여기를 봅니다.
> 그래서 **폴더 이름을 제가 정해드리는 대로** 똑같이 만들어야 합니다. 여러분 마음대로 만들면 제가 나눠드리는 스크립트가 안 돌아갑니다.
> 그리고 같은 작업을 **두 번** 합니다. 한 번은 마우스로 클릭해서, 한 번은 검은 화면에 명령어를 쳐서. 왜 두 번 하냐면, 앞으로 12주 동안 여러분이 쓰게 될 건 **마우스가 아니라 명령어**이기 때문입니다. 오늘 손에 익혀둡시다.

### 수행 항목

**1. S3 버킷 생성 (10분)**

1. 콘솔 검색창에 `S3` → S3 서비스로 이동.
2. **버킷 만들기(Create bucket)** 클릭.
3. 항목을 다음과 같이 정확히 입력한다.

| 항목 | 값 |
|---|---|
| AWS 리전 | **아시아 태평양(서울) ap-northeast-2** |
| 버킷 이름 | `mlops-2026-<학번>` (예: 학번이 20231234면 `mlops-2026-20231234`) |
| 객체 소유권 | ACL 비활성화됨 (기본값 유지) |
| 퍼블릭 액세스 차단 | **모든 퍼블릭 액세스 차단 — 체크 유지** |
| 버킷 버전 관리 | 비활성화 (기본값) |
| 기본 암호화 | SSE-S3 (기본값) |

4. **태그** 항목에서 다음 3개를 추가한다. (비용 추적용 — 필수)

| 키 | 값 |
|---|---|
| `Course` | `MLOps` |
| `Team` | `T00` (팀 미정이므로 임시. 3주차에 수정) |
| `Owner` | `<학번>` |

5. **버킷 만들기** 클릭.
- 확인 포인트: 버킷 목록에 `mlops-2026-<학번>` 이 보이고, AWS 리전 열이 `아시아 태평양(서울) ap-northeast-2` 이면 성공.

> ⚠ **버킷 이름은 전 세계에서 유일해야 한다.** 지구상 모든 AWS 사용자가 같은 이름 공간을 쓴다.
> `mlops` 나 `test-bucket` 같은 이름은 100% 실패한다. 반드시 `mlops-2026-<본인 학번>` 형식을 지킨다.
> 소문자·숫자·하이픈만 되고, 대문자와 밑줄(`_`)은 못 쓴다.

**2. 표준 폴더 구조 만들기 (10분)**

1. 방금 만든 버킷을 클릭해 들어간다.
2. **폴더 만들기(Create folder)** 로 다음 3개를 만든다. 이름을 정확히 지킨다.

| 폴더 | 용도 | 규칙 |
|---|---|---|
| `raw/` | 받은 그대로의 원본 데이터 | **절대 수정·삭제 금지** |
| `processed/` | 전처리를 마친 데이터 | 스크립트로만 생성 |
| `models/` | 학습된 모델 파일 | 4주차부터 사용 |

- 확인 포인트: 버킷 안에 세 폴더가 보이면 성공.

**강의 스크립트 (이 단계에서 붙이는 설명)**
> 왜 이렇게 나누냐고요? 3주차에 자세히 하겠지만 한 줄만 미리 말씀드립니다.
> **`raw/`는 성역입니다.** 원본은 절대 손대지 않습니다. 원본을 고쳐버리면 "어? 아까랑 결과가 다른데?" 하는 순간 되돌릴 방법이 없습니다.
> 원본은 그대로 두고, 가공한 결과를 `processed/`에 새로 씁니다. 그러면 스크립트를 다시 돌리기만 하면 언제든 같은 결과가 나옵니다. 이걸 **재현성**이라고 하고, 이 과목이 처음부터 끝까지 미는 가치입니다.

**3. 콘솔에서 업로드/다운로드 (10분)**

1. 강사가 배포한 `sample_bike.csv` 를 노트북에 저장한다.
2. `raw/` 폴더로 들어가 **업로드(Upload)** → 파일 추가 → `sample_bike.csv` 선택 → **업로드**.
3. 업로드된 파일을 클릭해 **객체 URL**과 **S3 URI**를 확인한다. S3 URI는 이런 모양이다.
```text
s3://mlops-2026-<학번>/raw/sample_bike.csv
```
4. **다운로드** 버튼을 눌러 다시 받아본다.
5. 객체 URL을 새 탭에 붙여넣어 본다. → **AccessDenied**가 뜬다.
- 확인 포인트: 마지막 단계에서 AccessDenied가 뜨는 게 **정상**이다. 퍼블릭 액세스를 차단해뒀기 때문. "인터넷에 있다고 아무나 볼 수 있는 게 아니다"를 여기서 짚는다.

**4. CloudShell에서 CLI로 같은 작업 반복 (25분)**

> CloudShell = AWS가 브라우저 안에서 띄워주는 리눅스 터미널. 설치할 게 없고 내 권한이 이미 적용되어 있다.

1. 콘솔 오른쪽 위 **`>_` 아이콘**(CloudShell)을 클릭한다. 검은 창이 열릴 때까지 30초~1분 기다린다.
2. 먼저 내가 누구인지 확인한다.
```bash
aws sts get-caller-identity
```
출력에 `"Arn": "arn:aws:iam::...:user/mlops-2026-<학번>"` 이 보이면 정상.

3. 현재 리전을 확인한다.
```bash
aws configure get region
```
`ap-northeast-2` 가 나와야 한다. 아무것도 안 나오거나 다른 값이면 아래를 실행한다.
```bash
aws configure set region ap-northeast-2
```

4. 내 버킷 목록을 본다.
```bash
aws s3 ls
```

5. 버킷 안을 본다. (`<학번>`은 본인 학번으로 바꿀 것)
```bash
aws s3 ls s3://mlops-2026-<학번>/
```

6. 폴더 안을 본다.
```bash
aws s3 ls s3://mlops-2026-<학번>/raw/
```

7. CloudShell 안에서 테스트 파일을 하나 만든다.
```bash
echo "hello mlops from <학번>" > hello.txt
cat hello.txt
```

8. 만든 파일을 S3에 올린다. (`cp` = copy, 복사)
```bash
aws s3 cp hello.txt s3://mlops-2026-<학번>/raw/hello.txt
```
`upload: ./hello.txt to s3://...` 메시지가 나오면 성공.

9. 올라간 것을 확인한다.
```bash
aws s3 ls s3://mlops-2026-<학번>/raw/
```

10. 이번엔 반대로 내려받는다. 다른 이름으로 받아 확인한다.
```bash
aws s3 cp s3://mlops-2026-<학번>/raw/hello.txt hello_download.txt
cat hello_download.txt
```

11. 아까 콘솔로 올린 CSV도 CLI로 내려받아 앞부분을 본다.
```bash
aws s3 cp s3://mlops-2026-<학번>/raw/sample_bike.csv .
head -5 sample_bike.csv
```

12. 폴더 통째로 다루기도 해본다. (`--recursive` = 하위 전부)
```bash
mkdir -p backup
aws s3 cp s3://mlops-2026-<학번>/raw/ backup/ --recursive
ls -l backup/
```

13. 마지막으로 버킷 전체를 한 번에 훑는다.
```bash
aws s3 ls s3://mlops-2026-<학번>/ --recursive --human-readable --summarize
```
- 확인 포인트: 맨 아래에 `Total Objects: 2` (또는 3), `Total Size: ...` 가 나온다. **이 화면을 캡처**한다. (체크포인트 제출물 2번)

14. (여유 있는 학생만) 방금 만든 테스트 파일을 지워본다.
```bash
aws s3 rm s3://mlops-2026-<학번>/raw/hello.txt
aws s3 ls s3://mlops-2026-<학번>/raw/
```

**강의 스크립트 (CLI 도입 시점)**
> 방금 마우스로 한 걸 이제 명령어로 다시 합니다. "굳이 왜?" 싶으실 겁니다.
> 이유는 간단합니다. 여러분이 4주차에 모델을 100개 만들었다고 칩시다. 마우스로 100번 업로드하시겠습니까? 스크립트 한 줄이면 끝납니다.
> 그리고 더 중요한 이유 — **12주차에 자동 배포를 배웁니다.** 자동화라는 건 사람이 마우스를 안 잡는다는 뜻입니다. 컴퓨터는 마우스를 못 씁니다. 명령어만 씁니다.
> `aws s3 cp` 이 명령어 하나만 오늘 손에 익히면 됩니다. `cp`는 copy, 복사입니다. 왼쪽 걸 오른쪽으로 복사합니다. 왼쪽이 내 컴퓨터고 오른쪽이 S3면 업로드, 반대면 다운로드입니다. 그게 전부입니다.

### 팀 프로젝트 연결

- 오늘 만든 버킷 `mlops-2026-<학번>` 은 **학기 내내 여러분의 개인 작업 공간**이다.
- 3주차에 팀이 확정되면 팀 공용 버킷(`mlops-2026-team-T01` 형식)이 추가로 생기지만, **개인 버킷은 개인 체크포인트 제출 용도로 계속 유지**한다.
- `raw/` `processed/` `models/` 세 폴더 규칙은 **팀 버킷에도 그대로 적용**된다. 강사가 배포하는 모든 스크립트가 이 경로를 가정한다.
- 오늘 적어둔 **버킷 이름**은 다음 주 boto3 실습에서 코드에 그대로 들어간다. 반드시 메모.

### 순회 지도 포인트

강사·조교가 돌아다니며 확인할 것 3가지.

1. **리전이 서울인가.** 버킷 목록 화면의 리전 열을 직접 본다. 버지니아면 그 자리에서 다시 만들게 한다.
2. **버킷 이름이 규칙대로인가.** `mlops-2026-<학번>` 형식인지, 대문자나 밑줄이 섞이지 않았는지 본다.
3. **CloudShell에서 `aws s3 ls`가 실제로 실행됐는가.** 콘솔만 만지고 CLI를 건너뛰는 학생이 반드시 나온다. 화면에 명령 실행 이력이 남아 있는지 본다.

추가로 확인: 태그 3종(`Course`/`Team`/`Owner`)을 빠뜨린 학생이 많다. 비용 추적이 안 되므로 그 자리에서 채우게 한다.

---

## 5. 체크포인트 (제출물) + 개별 트러블슈팅 버퍼 (45분)

### 제출물

제출처: 학습관리시스템(LMS) `1주차 체크포인트` 게시판. 파일명은 `w01_<학번>.zip` 또는 이미지 3장 개별 업로드.

| # | 제출물 | 형식 | 배점 |
|---|---|---|---|
| 1 | 본인 S3 버킷 이름을 텍스트로 기재 + 버킷 목록 화면(리전 열이 "서울"로 보이게) | 텍스트 + 스크린샷 | 0.7 |
| 2 | CloudShell에서 `aws s3 ls s3://mlops-2026-<학번>/ --recursive --human-readable --summarize` 실행 결과 화면 (명령어와 출력이 함께 보일 것) | 스크린샷 | 0.8 |
| 3 | 보안 자격 증명 화면의 MFA 디바이스 등록 목록 | 스크린샷 | 0.5 |
| **합계** | | | **2.0** |

> 제출 시 **화면 오른쪽 위의 사용자 이름과 리전이 함께 보이도록** 캡처한다. 이게 본인 확인 수단이다.
> 창을 잘라 캡처하면 본인 것인지 확인이 안 되어 재제출 요청 대상이 된다.

### 개별 트러블슈팅 버퍼 운용 (45분)

**진행 방식**
1. 체크포인트를 다 낸 학생은 **자리에서 손을 든다** → 조교가 명단에 ✅ 표시 → 아래 "먼저 끝난 학생 과제" 수행.
2. 조교는 미완료 명단을 화이트보드에 3개 그룹으로 적어 관리한다.
   - **A그룹 · MFA 미완료** — 앱 설치 문제. 강사 예비 기기 또는 브라우저 확장으로 처리
   - **B그룹 · 네트워크/브라우저 문제** — 핫스팟 전환, 시크릿 창, 확장 프로그램 해제 순으로 시도
   - **C그룹 · 리전/버킷 이름 오류** — 잘못 만든 버킷 삭제 후 재생성 (가장 빨리 처리 가능. 먼저 해치운다)
3. 강사는 A그룹, 조교는 B·C그룹을 맡는다. **한 명당 5분을 넘기면 다음 주 과제로 넘긴다.**
4. 종료 10분 전에 미해결 명단을 최종 확정하고, 해당 학생에게 **다음 주 수업 20분 전 조기 소집**을 통지한다.

**먼저 끝난 학생 과제 (지루하지 않게)**
- 부록 A 치트시트의 명령어를 **아무것도 안 보고** 3회 반복해 친다.
- 팀 구성 희망서를 그 자리에서 작성한다 (오늘 과제 선처리).
- 옆자리 미완료 학생을 돕는다. 도운 학생 이름을 조교에게 알리면 **참여 점수에 가산**.

### 평가 기준 (NCS 수행준거 연계)

- `2001070101_18v1.1` **인공지능 플랫폼 동향 분석하기(부분)**
  → 인공지능 서비스를 구축·운영하는 데 필요한 클라우드 플랫폼 구성요소(계정·권한·저장소·컴퓨팅)를 식별하고, 본 과목에서 사용할 플랫폼(AWS)의 구성 흐름을 15주 로드맵 위에서 설명할 수 있는가. (구두 확인 + 로드맵 위치 지목)
- `2001070201_18v1.3` **인공지능 서비스 기술 환경 분석하기(부분)**
  → 인공지능 서비스 구현에 필요한 기술 환경 요소인 리전·가용영역·권한(IAM)·객체 저장소(S3)의 역할을 구분해 설명하고, 본인 계정에 적용된 권한 범위(허용 서비스, 인스턴스 타입 제한)를 확인해 서술할 수 있는가. (제출물 1·3 + 순회 구두 확인)
- `2001070101_18v1.5` **인공지능 플랫폼 구축 비용 계획하기(부분)**
  → 온디맨드 과금 구조를 이해하고, 본인 계정의 월 예산 한도($10)와 알림 임계값(50/80/100%)을 확인해 비용 초과를 예방하는 조치(인스턴스 Stop, 리소스 정리)를 말할 수 있는가. (구두 확인 + Budgets 화면 확인)

> 채점 시 **정확도·완성도보다 "본인 계정에서 실제로 수행했는가"**를 본다. 1주차 체크포인트는 낙오자 조기 발견 장치이지 변별 장치가 아니다.

---

## 6. 정리 & 다음 주 예고 · 리소스 정리 안내 (5분)

### 오늘 배운 것 3줄 요약

1. MLOps는 "노트북 안에서 끝나는 모델"을 다른 사람이 쓸 수 있는 **서비스로 만드는 기술**이고, 우리는 15주에 걸쳐 그 한 줄기를 직접 관통한다.
2. AWS는 **리전(서울 `ap-northeast-2`)** 단위로 세상이 나뉘며, 리전이 다르면 내가 만든 것이 보이지 않는다.
3. 오늘 만든 **개인 S3 버킷 `mlops-2026-<학번>`** 과 `raw/` `processed/` `models/` 폴더 규칙은 학기 내내 쓰인다. 콘솔로도, `aws s3 cp/ls` 명령어로도 다룰 수 있다.

### 다음 주 미리보기 한 문장

> 다음 주에는 **여러분 전용 리눅스 서버(EC2)를 직접 한 대 만들어서** 검은 화면에 접속하고, 거기서 파이썬으로 오늘 올린 S3 파일을 읽어옵니다. 검은 화면 무서워하지 마세요, 명령어 15개만 쓰면 됩니다.

### 리소스 정리 타임 체크 항목

> 오늘은 **돈이 나가는 리소스를 만들지 않았다.** 하지만 다음 주부터는 매주 이 시간이 실제 점검 시간이 된다. 오늘은 습관을 들이는 리허설이다.

| # | 체크 항목 | 오늘 상태 |
|---|---|---|
| 1 | EC2 인스턴스가 켜져 있는가 → **Stop** | 오늘은 만들지 않음 (해당 없음) |
| 2 | S3 버킷 | **유지한다.** 삭제 금지. 저장 용량이 작아 비용이 사실상 0 |
| 3 | CloudShell 세션 | 창을 닫으면 자동 종료. 무료 |
| 4 | 브라우저 로그아웃 | 공용 PC 사용자는 **반드시 로그아웃** |
| 5 | 다음 주 준비 | 버킷 이름과 새 비밀번호를 메모해 뒀는가 |

**강의 스크립트**
> 오늘은 끌 게 없습니다. S3는 파일 몇 개로는 돈이 거의 안 나갑니다. 그러니 **버킷은 지우지 마세요.** 지우면 다음 주에 처음부터 다시 해야 합니다.
> 대신 지금 이 5분을 기억하세요. **다음 주부터 이 시간이 진짜입니다.** 서버를 켜놓고 가면 그때부터 돈이 샙니다. 매주 이 시간에 조교가 눈으로 확인합니다.
> 마지막으로, 공용 PC로 하신 분들은 지금 로그아웃하고 가세요. 다음 사람이 여러분 계정으로 서버를 만들 수 있습니다.

---

## 7. 과제

**제출 기한: 2주차 수업 시작 전까지 · LMS 업로드**

1. **팀 구성 희망서** (양식 `w01_팀구성희망서.docx`)
   - 함께하고 싶은 사람 이름 (없으면 "무관"이라고 적는다. 불이익 없음)
   - 관심 도메인 **1순위**를 아래에서 하나 고른다.
     - A. 수요·가격 예측 (회귀) — 따릉이 대여량, 전력 수요, 농산물 가격 등
     - B. 이탈·불량·리스크 분류 (분류) — 고객 이탈, 공정 불량 등 · **가장 쉬움**
     - C. 텍스트 리뷰 분석 (분류 + 생성형 AI) — 리뷰 감성 분류 + 요약 · **가장 흥미롭지만 어려움**
     - D. 자유주제 (사전 승인 필요 — 3주차에 조건 안내)
   - 본인이 해보고 싶은 역할: 데이터 / 모델 / 서빙 / 운영·문서 중 택 1
2. **미완료자 한정** — MFA 설정을 끝내고 인증 화면 캡처를 제출한다. **다음 주 수업 시작 전까지 필수.**
3. **선택(가산점 없음, 권장)** — 부록 A 치트시트의 `aws s3` 명령 5개를 CloudShell에서 한 번씩 더 쳐본다. 다음 주가 훨씬 편해진다.

---

## 부록 A. 명령어 치트시트

> **1주차 · AWS CLI (S3) 최소 명령어** — 1페이지로 뽑아 배포
> `<학번>` 자리에는 본인 학번 8자리를 넣는다. 예: `mlops-2026-20231234`

```bash
# ── 0. 나는 누구인가 / 어디에 있는가 ──────────────────
aws sts get-caller-identity          # 현재 역할과 계정 확인
aws configure get region             # 현재 리전 확인 (ap-northeast-2 여야 함)
aws configure set region ap-northeast-2   # 리전이 다를 때 고정

# ── 1. 목록 보기 (ls = list) ─────────────────────────
aws s3 ls                                             # 내 버킷 전체 목록
aws s3 ls s3://mlops-2026-<학번>/                      # 버킷 최상위
aws s3 ls s3://mlops-2026-<학번>/raw/                  # 특정 폴더 안
aws s3 ls s3://mlops-2026-<학번>/ --recursive --human-readable --summarize
                                                      # 전체를 한 번에 + 용량 합계

# ── 2. 올리기 / 내리기 (cp = copy) ────────────────────
aws s3 cp hello.txt s3://mlops-2026-<학번>/raw/hello.txt      # 업로드
aws s3 cp s3://mlops-2026-<학번>/raw/hello.txt ./             # 다운로드
aws s3 cp ./mydata/ s3://mlops-2026-<학번>/raw/ --recursive   # 폴더 통째로 업로드
aws s3 cp s3://mlops-2026-<학번>/raw/ ./backup/ --recursive   # 폴더 통째로 다운로드

# ── 3. 옮기기 / 지우기 ───────────────────────────────
aws s3 mv old.csv s3://mlops-2026-<학번>/raw/new.csv   # 이동(mv = move)
aws s3 rm s3://mlops-2026-<학번>/raw/hello.txt         # 파일 1개 삭제 (rm = remove)
aws s3 rm s3://mlops-2026-<학번>/tmp/ --recursive      # 폴더 통째로 삭제 ⚠ 주의

# ── 4. 동기화 (달라진 것만 복사) ─────────────────────
aws s3 sync ./mydata/ s3://mlops-2026-<학번>/raw/      # 로컬 → S3
aws s3 sync s3://mlops-2026-<학번>/raw/ ./mydata/      # S3 → 로컬

# ── 5. CloudShell 안에서 쓰는 리눅스 명령 (맛보기) ────
pwd            # 지금 내가 있는 폴더 경로
ls -l          # 파일 목록 (자세히)
cat hello.txt  # 파일 내용 전부 보기
head -5 a.csv  # 파일 앞 5줄만 보기
mkdir backup   # 폴더 만들기
```

**기억할 것 딱 하나**
`aws s3 cp <왼쪽> <오른쪽>` — 왼쪽 것을 오른쪽으로 복사한다.
왼쪽이 내 컴퓨터고 오른쪽이 `s3://`면 **업로드**, 반대면 **다운로드**.

---

## 부록 B. 용어 정리

| 용어 | 뜻 | 한 줄 설명 |
|---|---|---|
| MLOps | Machine Learning Operations | 모델을 노트북 밖으로 꺼내 실제로 굴리기 위한 운영 기술 전반 |
| AWS | Amazon Web Services | 아마존이 빌려주는 클라우드 컴퓨터·저장소·서비스 묶음 |
| 콘솔 (Console) | 관리 화면 | 브라우저로 접속해 마우스로 AWS를 다루는 웹 화면 |
| 리전 (Region) | 지역 | AWS 장비가 실제로 놓인 지구상 위치. **우리는 서울 `ap-northeast-2` 고정** |
| 가용영역 (AZ) | Availability Zone | 리전 안에 있는 개별 데이터센터 건물. 하나가 죽어도 나머지가 버팀 |
| 온디맨드 (On-Demand) | 쓴 만큼 과금 | 호텔방처럼 켜둔 시간만큼 초 단위로 돈이 나감 |
| 책임 공유 모델 | Shared Responsibility | AWS는 건물·전기·하드웨어, 사용자는 비밀번호·권한·포트를 책임짐 |
| IAM | Identity and Access Management | "누가 무엇을 할 수 있는가"를 관리하는 서비스. 우리 학생 계정이 여기 있음 |
| IAM 정책 (Policy) | 권한 규칙표 | 아파트 출입카드. 허용된 서비스·리소스만 열림 |
| MFA | Multi-Factor Authentication, 다단계 인증 | 비밀번호 + 1분마다 바뀌는 6자리 숫자. 계정 도용 방지 |
| S3 | Simple Storage Service | AWS의 파일 저장소. 폴더처럼 보이지만 실제로는 "객체 저장소" |
| 버킷 (Bucket) | S3의 최상위 저장 단위 | 이름이 **전 세계에서 유일**해야 함. 소문자·숫자·하이픈만 |
| 객체 (Object) | 버킷에 저장된 파일 하나 | 파일 내용 + 메타데이터를 합쳐 부르는 말 |
| S3 URI | `s3://버킷/경로` 형식 주소 | 코드와 CLI에서 파일을 가리킬 때 쓰는 표준 주소 |
| 퍼블릭 액세스 차단 | Block Public Access | 켜두면 인터넷 아무나가 내 파일을 못 봄. **항상 켜둔다** |
| CloudShell | 브라우저 안의 리눅스 터미널 | 설치 없이 바로 `aws` 명령을 칠 수 있는 검은 화면 |
| CLI | Command Line Interface | 마우스 대신 명령어로 조작하는 방식. 자동화의 전제 조건 |
| Budgets | 예산 | 월 사용액 한도와 알림을 설정하는 기능. 우리는 학생당 $10 |
| 태그 (Tag) | 리소스에 붙이는 이름표 | `Course=MLOps`, `Team=T01`, `Owner=<학번>`. 비용을 누가 썼는지 추적 |
| 인스턴스 (Instance) | 클라우드에서 빌린 서버 한 대 | 2주차부터 등장. `t3.medium`까지만 허용 |
| EBS | Elastic Block Store | 인스턴스에 붙는 하드디스크. **서버를 꺼도 요금이 계속 나감** |
| 재현성 (Reproducibility) | 같은 입력이면 같은 결과 | 원본(`raw/`)을 안 건드리고 스크립트로만 가공해야 확보됨 |

---

## 부록. AWS 화면과 공식 문서

![S3 버킷 생성 화면 예시](../assets/aws/s3-create-bucket.jpg)

> AWS 공식 문서에 포함된 화면 예시입니다. 화면 모양은 바뀔 수 있으므로 아래 메뉴 경로를 기준으로 찾습니다.

- 콘솔: <https://console.aws.amazon.com/s3/>
- 이동 경로: **S3 → Buckets → Create bucket**
- 화면에서 확인: 리전 `ap-northeast-2`, 퍼블릭 액세스 차단 유지, 고유한 버킷 이름
- 공식 문서: <https://docs.aws.amazon.com/AmazonS3/latest/userguide/create-bucket-overview.html>
- 계정 권장 방식: <https://docs.aws.amazon.com/IAM/latest/UserGuide/gs-identities.html>

```mermaid
flowchart LR
    A[IAM Identity Center 로그인] --> B[서울 리전 확인]
    B --> C[S3 버킷]
    C --> D[raw]
    C --> E[processed]
    C --> F[models]
```+

---

## 실제 AWS 콘솔 화면 실습 가이드 (1주차)

> 2026-08-24 서울 리전 콘솔에서 직접 캡처했다. 계정 번호가 보이는 오른쪽 상단은 제외했다. 화면 모양이 바뀌면 메뉴 경로와 확인 항목을 기준으로 찾는다.

### 서울 리전 콘솔 홈

![서울 리전 콘솔 홈](../assets/aws-console/week01/01-console-home-seoul.png)

- 콘솔 경로: **콘솔 홈 → 오른쪽 위 리전**
- 확인할 것: 아시아 태평양(서울)인지 확인
- [같은 화면 열기](https://console.aws.amazon.com/console/home?region=ap-northeast-2)

### IAM Identity Center 시작 화면

![IAM Identity Center 시작 화면](../assets/aws-console/week01/02-identity-center-home.png)

- 콘솔 경로: **IAM Identity Center → 대시보드**
- 확인할 것: 액세스 포털과 인스턴스 상태 확인
- [같은 화면 열기](https://ap-northeast-2.console.aws.amazon.com/singlesignon/home?region=ap-northeast-2)

### IAM 대시보드

![IAM 대시보드](../assets/aws-console/week01/03-iam-dashboard.png)

- 콘솔 경로: **IAM → Dashboard**
- 확인할 것: 보안 권고와 자격 증명 유형 확인
- [같은 화면 열기](https://console.aws.amazon.com/iam/)

### IAM 역할 목록

![IAM 역할 목록](../assets/aws-console/week01/04-iam-roles.png)

- 콘솔 경로: **IAM → Roles**
- 확인할 것: 학생 실습 역할 이름 확인
- [같은 화면 열기](https://console.aws.amazon.com/iam/home#/roles)

### S3 버킷 목록

![S3 버킷 목록](../assets/aws-console/week01/05-s3-bucket-list.png)

- 콘솔 경로: **S3 → Buckets**
- 확인할 것: 버킷 이름과 리전 확인
- [같은 화면 열기](https://console.aws.amazon.com/s3/)

### S3 버킷 만들기: 일반 구성

![S3 버킷 만들기: 일반 구성](../assets/aws-console/week01/06-s3-create-top.png)

- 콘솔 경로: **S3 → Buckets → Create bucket**
- 확인할 것: 서울 리전과 전역 고유 이름 확인
- [같은 화면 열기](https://s3.console.aws.amazon.com/s3/bucket/create?region=ap-northeast-2)

### S3 버킷 만들기: 객체 소유권

![S3 버킷 만들기: 객체 소유권](../assets/aws-console/week01/07-s3-create-ownership.png)

- 콘솔 경로: **Create bucket → Object Ownership**
- 확인할 것: Bucket owner enforced 확인
- [같은 화면 열기](https://s3.console.aws.amazon.com/s3/bucket/create?region=ap-northeast-2)

### S3 버킷 만들기: 퍼블릭 액세스 차단

![S3 버킷 만들기: 퍼블릭 액세스 차단](../assets/aws-console/week01/08-s3-create-public-block.png)

- 콘솔 경로: **Create bucket → Block Public Access**
- 확인할 것: 네 가지 차단 옵션 유지
- [같은 화면 열기](https://s3.console.aws.amazon.com/s3/bucket/create?region=ap-northeast-2)

### AWS Budgets 개요

![AWS Budgets 개요](../assets/aws-console/week01/09-budgets-overview.png)

- 콘솔 경로: **Billing → Budgets**
- 확인할 것: 월 예산과 알림 임계값 확인
- [같은 화면 열기](https://console.aws.amazon.com/billing/home#/budgets/overview)

### AWS CloudShell 시작 화면

![AWS CloudShell 시작 화면](../assets/aws-console/week01/10-cloudshell-home.png)

- 콘솔 경로: **상단 CloudShell 아이콘**
- 확인할 것: 서울 리전과 셸 준비 상태 확인
- [같은 화면 열기](https://ap-northeast-2.console.aws.amazon.com/cloudshell/home?region=ap-northeast-2)
